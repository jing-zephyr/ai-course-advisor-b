# 大模型配置（LLM_API_KEY 必填，其余有默认值）
# 默认使用 DeepSeek（platform.deepseek.com），可替换为任何 OpenAI 兼容端点（智谱GLM/通义/Moonshot 等）
LLM_API_KEY=your_api_key_here
LLM_BASE_URL=https://api.deepseek.com/v1
LLM_MODEL=deepseek-chat

# 本地服务端口（可选，默认 3000）
# PORT=3000
